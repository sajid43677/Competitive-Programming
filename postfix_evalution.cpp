#include<bits/stdc++.h>
using namespace std;

void main(){
    string s;
    s = s + ")";
    char a,b,op;
    stack st;
    for(int i = 0;;i++){
        if(s[i] == ')'){
            break;
        }
        else if((s[i] >= '0' && s[i] <= '9')||(s[i] =))
    }

}
